package com.lyrashop.user.repository;import java.time.Instant;import java.util.*;import org.springframework.data.jpa.repository.JpaRepository;import com.lyrashop.user.entity.NewsletterSubscription;public interface NewsletterRepository extends JpaRepository<NewsletterSubscription,UUID>{Optional<NewsletterSubscription> findByEmail(String email);Optional<NewsletterSubscription> findByConfirmationTokenHashAndConfirmationExpiresAtAfter(byte[] hash,Instant now);Optional<NewsletterSubscription> findByUnsubscribeTokenHash(byte[] hash);}
